---
# ============================================================================
# Playbook: RMAN Backup con Validazione
# Esegue backup completo + crosscheck + validate + report
# Uso: ansible-playbook -i inventory/production.ini playbooks/05_rman_backup.yml
# ============================================================================

- name: "Oracle RMAN — Backup Completo + Validate"
  hosts: oracle_primary[0]
  become: yes
  become_user: oracle
  gather_facts: yes
  roles:
    - role: oracle_rman_backup
      tags: [backup, rman]
