# Inventory dinamico minimo basato su output Terraform esportato in JSON.
# Uso locale:
# ansible-inventory -i automation/inventory/proxmox_dynamic_inventory.yml --list
plugin: ansible.builtin.yaml

all:
  children:
    control_plane:
      hosts:
        lab-vm01:
          ansible_host: 192.168.56.201
    workers:
      hosts:
        lab-vm02:
          ansible_host: 192.168.56.202
        lab-vm03:
          ansible_host: 192.168.56.203
